export const getters = {
  getDataReady: state => state.dataReady,
  getCurrentSection: state => state.currentSection,
  getProgramTypes: state => state.programType,
  getNestedData: state => state.nestedData
}

export default getters
